# Defaults for puppet - sourced by /etc/init.d/puppet
# This file is managed by Puppet, changes will be short lived

# Start puppet on boot?
START=<%= puppetstart %>

# Startup options
DAEMON_OPTS=""
